

modules:
  nose:
    ignore-unknown-actions: true
execution:
  - executor: jmeter
    scenario: simple_script
    blazegrid: true
    iterations: 1
    capabilities: 
      browserName: chrome

scenarios:
  simple_script:
    script: BlazeDemo.jmx

